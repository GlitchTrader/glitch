{
  "name": "glitch-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "python -m unittest discover -s tools/tests -p \"test_*.py\" && python -m unittest discover -s tools/hermes/tests -p \"test_*.py\""
  }
}
