# Lane Briefs — delegation prompts (relaunch-ready)

> Historical archive from 2026-07-07. Do not relaunch these prompts verbatim: paths, branches, classes, and backlog IDs have changed. Use `now.md` and `backlog.md` for current work. In particular, there is no current `PropFirmRuleLoader.cs`.

Written 2026-07-07 after the first four lanes were killed mid-flight by a subscription session limit. Any lead agent can respawn a lane by giving the brief below verbatim to a subagent (models: LANE-1/2 = Opus, LANE-3/4 = Sonnet; run in background). Shared rules for ALL lanes at the bottom.

## LANE-1 · replication-audit (Opus) → `docs/ledger/audits/replication-audit.md`

Audit GL-001 (master↔follower drift/delays; Glitch PnL ≠ NT PnL) and GL-004 (loops/fake/dup orders) root causes. Read deeply: `Services/Trading/GlitchReplicationEngine.cs`, `UI/MainWindow/GlitchMainWindow.Replication.partial.cs`, `Services/Risk/GlitchComplianceEngine.cs`, `Services/GlitchShellBridge.cs`, `Services/Persistence/GlitchStateStore.cs` + `GlitchRuntimePolicyStore.cs`, `ninjatrader/Glitch/Docs/architecture.md` + `data-flow-and-bridge.md`. Read-only evidence: `C:\Users\alan\Documents\NinjaTrader 8\` trace/log/GlitchData. Produce: (1) event-flow map with file:line refs; (2) every Glitch-PnL vs NT-PnL divergence point (Account.Get vs internal math, unrealized/realized, commissions, tick rounding, aggregation, **CultureInfo decimal-comma parsing** — pt-BR formatting is visible in prod screenshots); (3) duplication/loop analysis (idempotency, reentrancy, follower-fill echo, partial fills, rejections, reconnects, OrderUpdate/ExecutionUpdate ordering not guaranteed); (4) latency/drift analysis (sync vs dispatched, UI-thread marshaling, timers); (5) ranked findings — severity, file:line, concrete failure scenario, minimal fix + risk, CONFIRMED vs PLAUSIBLE; (6) repro/replay harness design. *Progress notes from killed runs: (run 1) PnL is sourced from `Account.Get(...)`; (run 2, was at report-writing stage with "comprehensive evidence") — CONFIRMED: follower Ratio/cell WPF bindings have no `ConverterCulture` → WPF binding parses with en-US while display is pt-BR (comma-decimal corruption surface for user-edited ratio cells). Re-verify and write the full report; most reading is likely re-doable quickly.*

## LANE-2 · math-audit (Opus) → `docs/ledger/audits/pnl-math-audit.md`

Audit GL-005 ("math must be infallible"). Read: `Services/Insights/GlitchTradeLedgerService.cs`, `GlitchTradeInsightsService.cs`, `GlitchRiskLockLedgerService.cs`, `UI/MainWindow/GlitchMainWindow.JournalTab.partial.cs` + `.SummaryTab.partial.cs`, `Services/Risk/` (drawdown/buffer/daily-loss). Verify: PF/win-rate/avg formulas incl. breakeven handling; trade construction from executions (partials, scale-in/out, FIFO vs flat-to-flat); MNQ math ($2/pt, $0.50/tick) and whether point value is hardcoded vs Instrument.MasterInstrument (gates GL-008 multi-asset); commissions consistency Journal vs tiles vs NT; **CultureInfo on every parse/format** (comma-decimal corruption of edited cells); multi-account rollups ("2 accounts" rows — double counting?); daily-boundary timezone. Sanity-check against prod screenshot: 94 trades, net −2430.25, WR 59.6%, AvgW +250.50, AvgL −433.11, PF 0.85, Long −3196.00 + Short +765.75 = −2430.25. Produce: formula inventory (metric → file:line → formula → verdict), ranked findings, and a testability plan (extract pure math into an NT-independent static class + concrete unit-test list). *Progress note from killed run: was confirming tab instantiation/construction order.*

## LANE-3 · ui-calm (Sonnet) → C# patches + `docs/ledger/audits/ui-calm-changes.md`

Implement GL-010→GL-015 in strict order, stop when confidence drops: (1) GL-010 scrollable panels for 20×20 accounts/followers (`.SummaryTab.partial.cs`, `.Replication.partial.cs`, also check `DashboardTab.partial.cs` — killed run found `ConfigureDataGridScrolling` already exists on DataGrids and Connected Accounts lives in DashboardTab); (2) GL-013 Journal: Performance primary, Live Feed demoted/collapsed (`.JournalTab.partial.cs`); (3) GL-012 warning severity split — only imminent-breach/lock/flatten render red persistent, all else quiet colorless notices, zero red in a normal sim session (find generation source, add severity there); (4) GL-015 editable-cell affordance (hover + glyph) + ratio-math tooltip in follower table; (5) GL-011 followers-first dashboard reorder (simple reorder preferred) only if 1–4 clean; (6) GL-014 settings granularity DESIGN ONLY in the change doc. Style-match existing code; localization via GlitchLocalizationService/TSV as neighbors do (UTF-8!); surgical, no refactors; cannot compile → mirror existing WPF patterns, re-read every edit; document per-item what a reviewer must verify after NT8 compile. *Progress notes: run 2 landed ONE surviving edit (uncommitted in working tree, lead-reviewed and approved 2026-07-07): `DashboardTab.partial.cs` connected-accounts DataGrid `MaxHeight = 240` + GL-010 comment — premise verified (grid is row1 Auto above star-sized followers row2; existing `ConfigureDataGridScrolling` engages). Do NOT redo that edit; continue from it: follower group tables in `.Replication.partial.cs`, then GL-013/012/015/011 per order. Also apply LANE-1's finding on editable cells: bindings lack `ConverterCulture` (en-US parse vs pt-BR display).*

## LANE-4 · external-truth (Sonnet) → `docs/ledger/research/nt8-propfirm-refresh-2026-07.md`

GL-016 web research, cite every claim with URL, mark UNVERIFIED honestly. (A) NT8 current dev guidance: Order/Execution/Position event ordering, multi-account submission best practices, Account.Get(AccountItem.*) semantics vs NT UI display, 8.1.x breaking changes last ~18 months, AddOn threading/UI rules. (B) Per firm (Apex, TPT, TradeDay, FundingTicks, Lucid): CURRENT tiers, drawdown type + exact computation, daily loss, max contracts, consistency, min days, payout rules affecting trading, news restrictions, **copy-trading/automation policy with exact policy text (existential red flag)**, 2025–26 changes. (C) Diff vs repo (locate PropFirmRules.json / PropFirmRuleLoader.cs via grep; skim GlitchComplianceEngine.cs rule params) → table: encoded vs found vs action. *Progress note from killed run: was at report-writing stage; confirmed PropFirmRuleLoader consumes all JSON fields needed for the diff.*

## Shared rules (all lanes)

- Read `docs/ledger/north-star.md` + `docs/ledger/backlog.md` first. Repo: `D:\ab\projects\Glitch\Glitch-Platform`.
- Audit/research lanes: NO code changes, write only your one output file.
- NEVER write under `C:\Users\alan\Documents\NinjaTrader 8\` (read-only evidence).
- No `git commit` — the lead reviews and commits.
- Evidence before inference; CONFIRMED vs PLAUSIBLE on every finding; honest uncertainty.
