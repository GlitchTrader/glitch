# Glitch App

Next.js app workspace in the Glitch monorepo. Currently a minimal Next.js 16 + React 19 placeholder. Run from repo root:

```bash
npm run dev --workspace apps/app
```

Build:

```bash
npm run build --workspace apps/app
```

See [Next.js documentation](https://nextjs.org/docs) for framework details.
